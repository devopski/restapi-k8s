apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: restapi
  namespace: restapi
spec:
  host: dev-restapi.com
  path: /
  to:
    kind: Service
    name: restapi-service
  port:
    targetPort: 8080